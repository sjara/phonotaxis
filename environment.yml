name: phonotaxis
channels:
  - conda-forge
  - defaults
dependencies:
  - python>=3.9
  - pyqt>=6
  - python-sounddevice
  - pip
  - pip:
      # Use pip to install your project itself, and any dependencies not on conda-forge
      - -e .  # This installs your project in editable mode

# You may need to install opencv-python from pip by yourself